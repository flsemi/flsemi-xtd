[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "flsemi-xtd"
version = "0.1.3"
description = "Local tools for the FLSEMI BFi91XTD DECT NR+ evaluation kit on Nordic Thingy:91 X — dashboard, configuration and firmware update over USB or Bluetooth LE"
readme = "README.md"
license = {text = "BSD-3-Clause"}
requires-python = ">=3.9"
authors = [
  {name = "FL Semiconductor LLC", email = "hello@flsemi.us"},
  {name = "BANFi Semiconductor Co., Ltd."},
]
keywords = ["DECT NR+", "DECT-2020", "nRF9151", "nRF5340", "Thingy:91 X", "mesh", "SMP", "mcumgr"]
classifiers = [
  "License :: OSI Approved :: BSD License",
  "Programming Language :: Python :: 3",
  "Operating System :: OS Independent",
  "Topic :: System :: Hardware",
  "Intended Audience :: Developers",
]
dependencies = ["pyserial>=3.5", "cbor2>=5.4", "bleak>=0.21"]

[project.urls]
Homepage = "https://flsemi.us/bfi91xtd"
Source = "https://github.com/flsemi/flsemi-xtd"

[project.scripts]
xtd = "xtd.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
